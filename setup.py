from setuptools import setup, find_packages

setup(
    name='arrays_and_hashing',
    version='0.0.0',
    packages=find_packages(),
)
setup(
    name='sliding_window',
    version='0.0.0',
    packages=find_packages(),
)
setup(
    name='two_pointers',
    version='0.0.0',
    packages=find_packages(),
)
setup(
    name='stack',
    version='0.0.0',
    packages=find_packages(),
)